@namespace ArtifactBrowser.Client.Components
@using ArtifactBrowser.Client.Utilities

<div class="markdown-view">@((MarkupString)MarkdownSafeHtml.ToHtml(Content))</div>

@code {
    [Parameter]
    public string? Content { get; set; }
}
